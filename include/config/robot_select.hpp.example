/**
 * @file robot_select.hpp.example
 * @brief Template for local robot selection (copy to robot_select.hpp).
 *
 * robot_select.hpp is gitignored so each developer picks Blue or Orange without
 * committing the choice. Uncomment exactly one #define before building.
 */

#pragma once

// #define ROBOT_BLUE
// #define ROBOT_ORANGE

#error "Select exactly one robot by uncommenting the corresponding define."
